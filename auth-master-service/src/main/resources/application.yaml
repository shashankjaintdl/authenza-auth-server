

app:
  datasource:
    url: jdbc:mysql://localhost:3306/auth-master?createDatabaseIfNotExist=true
    dbType: MYSQL
    username: root
    password: sjain@123
    driverClassName: com.mysql.cj.jdbc.Driver
  system-tenant:
    url: jdbc:mysql://localhost:3306/system_admin?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true

spring:
  data:
    redis:
      host: localhost
      port: 6379
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/migration/master/db.changelog-master.xml
    test-rollback-on-update: false   # Disable testing rollback on update to prevent table deletion after deploy
    default-schema:
      # Use this to prevent Liquibase from running if there's a checksum mismatch
#    on-error: HALT